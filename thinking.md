# 十月文化内部的首发包流程构思

> 这里的收发包流程仅仅是十月内部的自动化流程，不包括提供给外包的外包工具集

> 理论上来讲对于外包这块内部的自动化流程的搭建要优先于为外部提供工具，因为外包商的素质良莠不齐，导致不同的外包商甚至个人外包对十月内部提供的工具依赖程度是不一样的，很可能出现外包商基本不用我方提供的工具也可以在计划时间内完成工作任务的情况，对于这种类型的外包商，我们很难从外部工具来强约束其回传行为，那么更加稳妥的方式就是给内部搭建一个尽可能自动化检查自动化分发、处理数据的流程来弱化我们内部的效率压力。尽可能做好就算完全不给外部提供任何工具只提供制作数据的情况下也能将外包工作完整分发回收的内部工作流程。


---

## 涉及到要发包的环节：


* 资产部分

         模型环节 材质环节 毛发环节 绑定环节

* 镜头部分

        layout环节 animation环节 efx环节 cfx解算环节 灯光环节 合成环节


---

## 收发包设计到的功能模块



* 传输模块（部署linux）

        直接部署使用ftps即可

* 数据库模块（部署linux）

        来记录ftp文件信息以及历史的数据库，暂定postgreSQL


* 收集模块（windows控制端）

        根据不同的环节，收集整理必要的依赖项到ftps的共享存储中提供给外包人员进行下载

* 版本监控模块（linux服务）

        会有一个无限循环的进程不断扫描ftp共享出来的存储空间和ftps的服务日志，为每一次更改改动（事无巨细地）来打标记到数据库中，主要记录上传了什么、谁上传的、上传了多大、是否对原有文件产生了覆盖，如果覆盖了则会自动在数据库里标记此次上传是v002版本，删除了什么、谁删除的

* 权限分配模块（windows控制端）

        会有一个工具来专门做用户管理和实时控制用户权限，来动态的对某些用户赋予能查看哪些文件、下载哪些文件的权利，或者新增外包商用户等等；对于新增的外包商会自动在shotgun为其创建创建用户数据并记录用于联系的钉钉ID，并在linux中也同时为其创建用户同步到ftp中，并且自动生成根据用户名自动生成一个密码并将这些ftp链接端口用户名密码等信息通过钉钉一并发送给外包商


* 行为分配模块（linux服务）

        这个模块主要会有一个无限循环进程来监听文件数据库的变化，一旦产生增或者改的行为就会根据数据库里的变化的数据去查找对应的文件，整合分辨出哪些改动属于一次的改动，哪些改动属于同一时间另一个人的改动, 查看这些文件隶属哪个环节，将所有的这些信息进行分类整理，如果没有改动完还正处于改动中的状态就等待全部改动完毕，然后给行为模块发送这个环节的行为命令和这次改动的文件信息
        
        

* 自动行为模块（linux服务）

        这个模块主要包含不同环节的自定义检查和行为代码，比如：是否按照环节路径层级规范进行摆放，根据不同的环节监测行为代码来对文件进行合规检查（检查预览视频和否帧率帧数正确等，甚至自动发布daily和下游并钉钉通知外包方检验进度），并且提供队列功能和用农场节点来处理耗时任务的功能，对于同一时间挤进来的大量待处理队列进行有序等待，并在处理队列任务时将大量耗时的部分扔到农场来代替执行，等待执行结果来完成最终执行反馈。

---

## 一个理想情况下完整的发收包流程

* 制片在十月盒子中打开权限分配工具，添加外包商并为其指定权限

* 制片在十月盒子中资产收集工具，半自动的找到所有参考数据，点击确定后由工具自动将收集的数据发送至ftp存储路径下

* 外包商制作完毕后按照不同环节的规范向ftp中回传审核数据

* 版本监控模块 识别到新的文件->行为分配模块 等待此次上传全部结束并将这些信息打包->自动行为模块 开始识别该环节，并根据该环节下TD编写的自动行为指令开始检查并发布

* 自动行为模块 检查发现程序运行错误->钉钉给TD发送报错内容，检查发现 文件检查错误 ->钉钉给外包商发送审核不通过的消息并指明其原因，并同时发给相应环节制片消息

* 外包商再次对ftp进行回传，按照自动流进入到 自动行为模块 检查通过，给该环节制片发送通过消息，如有需求根据自动工具的设置发布到shotgun上， 最后给外包商提示回传成功的消息

* 审核确定后最终外包商提交最终发布的工程文件/数据缓存/预览图片等，后根据外包商提供的manifest数据，在进入到 自动行为模块 时触发自动发布下游程序，由新的发布工具来使用cli的方式进行全自动检查发布，同上无论发布成功与否都会钉钉通知外包商和制片。

---

## ftps路径层级设计

> 一些要求 
>> 所有的外包回传文件（审核用预览视频/图片除外）必须在project文件内包含manifest.json文件，用于记录一些必要的信息

    ./root
        /oct
            /mk2
                /shot
                    /s03
                    /s04
                        /s040040
                            /asb
                            /lay
                                /..
                            /ani
                        /..
                    /..
                /asset
                    /chr
                        /dagui_c
                            /asb
                                /dagui_c.usda
                            /art
                                /..
                            /mod
                                /dagui_c.mod.model
                                    /dagui_c.ma
                                    /mesh.xml
                                    /usd
                                        /comp.usda
                                        /high.usd
                            /rig
                            /tex
                            /fur
                    /asb
                    /prp

        /vender01
            /dagui_g
                /mod
                    /preview
                        /xxxx.png
                        /xxx.mov
                    /project
                        /manifest.json
                        /images
                        /dagui_g.ma
                /tex
                /fur
                /rig
            
            /s060050
                /lay
                    /preview
                        /xxxx.mov
                    /project
                        /manifest.json
                        /s050060.lay.rough_layout.v018.ma
                /ani
                /efx
                /cfx
                /lgt
                /cmp
        
        /vernder02
        /..